<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.s3.s3">

    <application
        android:name=".S3Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_brightness_7_black_24dp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SavingsActivity" />
        <activity android:name=".SystemTweakerActivity" />
        <activity android:name=".EPCListActivity"></activity>
    </application>

</manifest>